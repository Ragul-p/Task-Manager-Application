<h6>Dashboard</h6>
<h3>Welcome {{name}} !</h3>
<p>Here are your task list</p>
{{#if tasks}}
<table class="striped">
    <thead>
        <tr>
            <th>Task-Title</th>
            <th>Task-Created at</th>
            <th>Task-Status</th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        {{#each tasks}}
        <tr>
            <td><a href="/tasks/{{_id}}">{{title}}</a></td>
            <td>{{formatDate createdAt 'MMMM Do YYYY, h:mm:ss a'}}</td>
            <td><span class="dash-status">{{status}}</span></td>
            <td>
                <a href="/tasks/edit/{{_id}}" class="btn btn-float">
                    <i class="fas fa-edit"></i>
                </a>

                <form action="/tasks/{{_id}}" method="POST" id="delete-form">
                    <input type="hidden" name="_method" value="DELETE">
                    <button type="submit" class="btn red">
                        <i class="fas fa-trash"></i>
                    </button>
                </form>
            </td>
        </tr>
        {{/each}}
    </tbody>
</table>
{{else}}
<img src="{{image}}" class="circle responsive-img img-small">
<p>You have not any task list. Please Click on <span style="color: red; font-weight: bold; font-size: 24px;">+</span>
    and Create Your Task</p>
{{/if}}